## 1. Comparison chart polish

- [x] 1.1 Increase the comparison chart frame size and tune the doughnut cutout so the rings read larger and thinner.
- [x] 1.2 Update the comparison tooltip formatter so hover text explicitly names the ETF and the hovered metric category.
- [x] 1.3 Verify the comparison charts still render correctly across desktop and mobile widths.

## 2. Aggregated bar scaling

- [x] 2.1 Normalize aggregated company bar widths against the maximum total exposure in the current view.
- [x] 2.2 Preserve stacked contributor segments inside each bar while scaling the row width.
- [x] 2.3 Check that labels, chips, and bar geometry remain readable when rows shrink.

## 3. Validation

- [x] 3.1 Run targeted syntax checks for the touched web files.
- [x] 3.2 Open the browser view and confirm the updated comparison and aggregated layouts render as intended.
