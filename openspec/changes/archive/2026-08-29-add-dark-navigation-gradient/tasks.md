## 1. Theme Styling

- [x] 1.1 Add a semantic navigation background token for Bright mode and a restrained top-to-bottom dark gradient override in `web/styles.css`.
- [x] 1.2 Apply the navigation token to `.primary-navigation` while preserving sticky positioning, spacing, active-tab styling, and responsive behavior.

## 2. Verification

- [x] 2.1 Extend `tests/test_web_contract.py` with assertions for the navigation token and dark gradient coverage.
- [x] 2.2 Run focused web contract tests and manually verify the gradient and readable navigation at desktop and mobile-sized Dark-mode viewports.
