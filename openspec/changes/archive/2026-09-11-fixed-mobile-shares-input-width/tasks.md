## 1. Mobile Shares Input Sizing

- [x] 1.1 Change the mobile Selected positions Shares input from a fluid width to the established fixed `84px` width while preserving the responsive card grid.
- [x] 1.2 Verify the fixed-width input remains usable alongside Weight and Remove at supported mobile viewport widths.

## 2. Regression Coverage

- [x] 2.1 Extend the web contract test to assert the fixed mobile Shares-input width and preserve the existing wide-layout width contract.
- [x] 2.2 Run the focused web contract tests and validate the completed OpenSpec change.