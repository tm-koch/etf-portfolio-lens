## 1. Data and Exposure Model

- [x] 1.1 Confirm the aggregated company exposure data includes per-ETF contribution values for each holding.
- [x] 1.2 Normalize the company exposure output so each row can be rendered as a stacked set of ETF segments.

## 2. Stacked Bar View

- [x] 2.1 Replace the aggregated holdings list with horizontal stacked bars ordered by total company exposure.
- [x] 2.2 Render one segment per contributing ETF and show the segment percentage in the bar or detail text.
- [x] 2.3 Keep the total company exposure percentage visible at the row level.

## 3. Responsive and Accessibility Polish

- [x] 3.1 Tune the stacked bar layout for narrow screens so the aggregated view stays readable without horizontal scrolling.
- [x] 3.2 Verify hover or detail text still exposes exact ETF contribution values when labels cannot fit inline.
