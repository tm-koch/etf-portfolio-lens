## 1. Publish layout

- [x] 1.1 Define the root-hosted GitHub Pages publish tree for the frontend and data assets.
- [x] 1.2 Keep the `gh-pages` branch as the publish branch.
- [x] 1.3 Preserve the current root-absolute snapshot paths used by the catalog.

## 2. Deployment workflow

- [x] 2.1 Add or document a repeatable PowerShell publish script for the static site.
- [x] 2.2 Include the relevant `web/` files and `data/raw/<date>/snapshots` files in the publish output.
- [x] 2.3 Verify the deployed site can load the catalog and snapshots from the Pages root.

## 3. Validation

- [x] 3.1 Confirm the published tree matches the required file layout.
- [x] 3.2 Confirm the static site loads correctly from the root-hosted Pages URL.
- [x] 3.3 Validate the `gh-pages` branch contents after publishing.
