## 1. Mobile legend compactness

- [x] 1.1 Remove the smartphone-only full-width legend item rule so legend pills can share rows on small screens.
- [x] 1.2 Tune the mobile legend gaps and alignment so wrapping remains compact and readable.

## 2. Verification

- [x] 2.1 Verify the smartphone comparison legend displays multiple entries per row when space permits.
- [x] 2.2 Verify the comparison legend remains separate from the donut canvas and the chart data presentation is unchanged.
