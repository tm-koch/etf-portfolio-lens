## 1. Rendering update

- [x] 1.1 Remove the one-time clientWidth-based pixel width from the aggregated company bar track.
- [x] 1.2 Bind the bar track width to container-relative sizing so it responds automatically to resize.
- [x] 1.3 Preserve the stacked segment composition and company labels inside each row.

## 2. Validation

- [x] 2.1 Verify the aggregated view at narrow and wide viewport widths.
- [x] 2.2 Confirm resized bars stay fully inside the frame and preserve ranking order.
- [x] 2.3 Run the relevant frontend smoke check for the aggregated panel.
