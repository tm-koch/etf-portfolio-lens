## 1. Compact Table Palette

- [x] 1.1 Set the compact Explore table header and sticky Holding header backgrounds to `rgb(232, 236, 244)`.
- [x] 1.2 Add alternating odd/even resting backgrounds for compact Explore body rows using `rgb(244, 246, 251)` and white.
- [x] 1.3 Apply matching odd/even backgrounds to sticky holding-name body cells while preserving the existing hover overrides.

## 2. Verification

- [x] 2.1 Verify the compact preview renders the requested header and alternating row colors with representative holdings.
- [x] 2.2 Verify appended rows continue the alternating pattern and the sticky holding column stays synchronized during horizontal scrolling.
- [x] 2.3 Verify hover feedback, mobile overflow, stable numeric columns, and holding-name clipping remain unchanged.
- [x] 2.4 Run the available tests and confirm the OpenSpec change status is complete.
