## 1. Update web application branding

- [x] 1.1 Rename the browser title, hero label, meta description, and error fallback text in `web/index.html` and `web/app.js`.
- [x] 1.2 Update the local server banner and CLI description in `web/server.py`.
- [x] 1.3 Review the web README heading so it matches the new product name.

## 2. Update repository documentation

- [x] 2.1 Rename the top-level README title and any visible product references to ETF Portfolio Lens.
- [x] 2.2 Update `doc/product_idea.md` so the product name and framing use the new brand consistently.
- [x] 2.3 Search for lingering ETF Lens text in docs and web assets and remove or replace it.

## 3. Verify the rename

- [x] 3.1 Open the web app and confirm the new product name appears in the page chrome and fallback copy.
- [x] 3.2 Run a repository-wide text search for ETF Lens to ensure only intentional legacy references remain.
